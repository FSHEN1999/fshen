# DPU 项目长期自动化测试 / AI 测试治理方案

## 一、方案定位

本方案面向 DPU 项目中长期质量建设，目标不是单次补测试，而是建立一套可持续运行的“版本迭代质量保障机制”。

核心诉求包括：

1. 每次版本迭代后，都能自动完成回归测试
2. 每次测试后，都能由 AI 自动输出验证说明、问题分析、优化建议报告
3. 管理层可以快速看到本次版本是否可放行、风险在哪、投入产出是否持续改善
4. 测试体系能够随着 FP、HSBC、多店铺、线上/线下等业务演进持续扩展

这套方案本质上是把 DPU 的测试能力，从“脚本工具”升级为“版本治理能力”。

## 二、建设目标

建议将长期目标分为 4 个层次。

### 1. 版本级自动回归

目标：

- 每次版本迭代后自动触发测试
- 根据变更内容自动选择应跑的测试包
- 形成统一测试结果输出

结果：

- 减少人工点测和遗漏
- 缩短迭代后的验证周期

### 2. 质量级风险判断

目标：

- 让系统不仅告诉我们“测没测”，还要告诉我们“有没有问题”“风险在哪”

结果：

- 管理层可快速判断是否具备发布条件
- 测试团队可快速聚焦高风险模块

### 3. AI 级辅助决策

目标：

- 由 AI 自动汇总测试执行情况
- 自动说明本次版本验证了什么、发现了什么、缺了什么、下一步应优化什么

结果：

- 从“看日志、看脚本结果”升级为“看结论、看风险、看建议”

### 4. 治理级持续改进

目标：

- 逐步形成测试资产、缺陷数据、历史版本质量数据的沉淀
- 建立质量趋势分析和优化机制

结果：

- 测试能力可持续积累
- 质量改进可度量

## 三、适用 DPU 项目的长期建设思路

结合当前 DPU 项目特点，长期方案应围绕以下 5 条主线展开：

- FP-USD 主流程
- HSBC 主流程
- 多店铺专项流程
- 线上流程
- 线下流程

同时以现有能力为基础：

- `mock_sit.py` / `mock_uat.py` 负责状态流自动化
- `线上自动化.py` / `线下自动化.py` / `线下自动化hsbc.py` 负责关键 UI 路径自动化
- `db_helper.py` 负责数据库断言
- AI 负责测试点解释、结果总结、问题归因、优化建议

因此，DPU 长期方案不应只做 UI 自动化平台，而应做：

“状态流自动化 + UI 冒烟 + 数据库断言 + AI 质量报告”的组合体系。

## 四、总体蓝图

建议把长期能力设计为 5 层闭环。

### 第一层：版本触发层

作用：

- 接收每次版本迭代的触发信号
- 自动启动对应测试任务

触发方式建议：

- 代码合并到主分支后触发
- 测试环境部署完成后触发
- 预发布版本构建完成后触发
- 手动补跑触发

管理价值：

- 保证测试进入版本节奏，而不是依赖人工提醒

### 第二层：自动测试执行层

作用：

- 按变更范围自动执行相应测试包

建议测试包长期固定分为：

- FP-USD 回归包
- HSBC 回归包
- 多店铺专项包
- 线上冒烟包
- 线下冒烟包
- 公共状态流回归包
- 数据库断言包

管理价值：

- 每次测试可控、可复用、可扩展

### 第三层：结果采集层

作用：

- 收集测试结果、日志、截图、数据库断言结果、失败场景

建议采集内容：

- 用例通过率
- 失败用例列表
- 失败截图
- webhook 响应
- drawdown / repayment / PSP 等状态推进结果
- 关键数据库字段校验结果

管理价值：

- 结果结构化，便于复盘和 AI 分析

### 第四层：AI 分析报告层

作用：

- 将执行结果自动转化为人能读的报告

建议 AI 报告固定输出 4 类内容：

1. 本次版本验证了什么
2. 本次发现了哪些问题
3. 当前还有哪些风险和缺口
4. 下一步有哪些值得优化的点

管理价值：

- 从“技术结果”变成“管理结论”

### 第五层：质量治理层

作用：

- 沉淀版本质量趋势
- 沉淀问题类别和长期改进项

建议治理输出：

- 版本质量趋势图
- 自动化覆盖率趋势
- 假失败率趋势
- 高频失败模块排行
- 历史缺陷回归命中率
- AI 建议采纳率

管理价值：

- 形成中长期质量经营能力

## 五、每次版本迭代的理想工作流

建议未来每次版本迭代固定按照以下流程运行：

### 第 1 步：版本变更进入测试环境

动作：

- 系统自动识别本次版本内容
- 标记涉及模块，如 FP、HSBC、多店铺、locators、webhook、状态流等

### 第 2 步：自动选择测试范围

动作：

- 如果涉及 `mock_uat.py` / `mock_sit.py`，自动执行状态流回归
- 如果涉及 `locators.py` 或 UI 脚本，自动执行线上/线下 UI 冒烟
- 如果涉及多店铺脚本，自动执行多店铺专项包
- 如果涉及 HSBC 分支，自动执行 HSBC 回归包

### 第 3 步：自动执行测试

动作：

- 执行对应回归测试包
- 自动完成截图、日志、数据库断言、状态采集

### 第 4 步：AI 自动生成报告

输出内容建议固定为：

- 本次测试验证范围
- 执行摘要
- 失败项摘要
- 风险判断
- 是否建议放行
- 是否存在遗漏验证点
- 优化建议

### 第 5 步：管理层查看摘要

管理层只需关注：

- 本次版本是否通过基础质量门槛
- 哪些问题阻塞上线
- 哪些问题可接受但需跟踪
- 哪些方向值得投入优化

## 六、AI 报告建议长什么样

建议未来每次版本输出两类报告。

### 1. 管理层摘要版

建议包含：

- 版本名称 / 日期
- 本次验证范围
- 核心结果结论
- 风险等级
- 是否建议放行
- 本次发现问题数
- 本次建议优化项

特点：

- 1 页内可读完
- 结论先行
- 不堆底层日志

### 2. 测试执行分析版

建议包含：

- 执行了哪些测试包
- 每个测试包通过率
- 失败详情
- 失败可能原因
- 数据库断言结果
- AI 建议补测点
- 后续优化建议

特点：

- 面向测试 / 开发 / 项目负责人
- 便于后续整改

## 七、预期收益

从管理层视角，长期建设后的收益主要体现在 5 个方面。

### 1. 发布风险下降

每次版本发布前都有稳定回归和风险判断，减少带病上线概率。

### 2. 回归效率提升

把大量重复人工验证转为自动执行，缩短版本验收周期。

### 3. 质量透明度提升

测试结果从“零散脚本输出”变成“结构化质量报告”，便于管理层掌握真实状态。

### 4. 分支流程可控

对 FP、HSBC、多店铺、线上/线下等关键分支建立固定验证包，降低遗漏风险。

### 5. 测试资产沉淀

每次版本都在积累测试资产、问题模式、质量趋势数据，为长期优化提供依据。

## 八、投入建议

建议按“基础建设 + 持续运营”两类投入来规划。

### 1. 基础建设投入

主要包括：

- 自动化测试框架整理
- 状态流测试包建设
- UI 冒烟包建设
- 数据库断言封装
- 测试报告标准化
- AI 报告模板建设
- CI/CD 集成

特点：

- 前期投入相对集中
- 一旦成型，后续复用价值高

### 2. 持续运营投入

主要包括：

- 每个迭代新增或维护测试点
- 定期维护 locator 和数据断言
- 跟进失败用例与环境问题
- 迭代优化 AI 报告准确性
- 沉淀版本质量周报 / 月报

特点：

- 长期低强度持续投入
- 依赖制度化执行

## 九、实施里程碑建议

建议长期建设分 4 个阶段推进。

### 里程碑 1：基础回归可用

周期建议：0-2 个月

目标：

- 建立 FP-USD、HSBC、多店铺、线上、线下的基础测试包
- 形成基础执行入口

标志：

- 可以完成关键稳定流程的自动回归

### 里程碑 2：版本自动触发可用

周期建议：2-4 个月

目标：

- 打通版本触发机制
- 每次版本后自动执行测试

标志：

- 测试正式进入版本节奏

### 里程碑 3：AI 报告可用

周期建议：4-6 个月

目标：

- 每次版本后自动生成 AI 测试报告
- 提供验证说明、问题分析、优化建议

标志：

- 管理层可直接看摘要报告做判断

### 里程碑 4：质量治理可用

周期建议：6-12 个月

目标：

- 建立版本质量趋势和治理机制
- 形成质量看板和长期优化闭环

标志：

- 质量建设从“项目动作”升级为“常规经营动作”

## 十、管理建议

为了保证长期方案真正落地，建议同时建立以下管理机制：

### 1. 版本准入机制

建议明确：

- 哪些测试包必须通过才能发布
- 哪些问题属于阻塞级问题
- 哪些问题可以带风险上线但需跟踪

### 2. 变更联动机制

建议明确：

- UI 变更必须联动 UI 冒烟
- 状态流变更必须联动状态流回归
- 多店铺变更必须联动多店铺专项
- HSBC 变更必须联动 HSBC 回归

### 3. 报告固定化机制

建议每次版本固定输出：

- 管理层摘要版
- 执行分析版

### 4. 持续复盘机制

建议按月复盘：

- 哪些问题自动化提前发现了
- 哪些问题仍然遗漏
- 哪些测试包最有价值
- 哪些测试点或 AI 报告需要优化

## 十一、最终建议

从 DPU 项目实际情况看，长期最适合建设的不是单点自动化，而是一套围绕版本迭代运行的质量治理机制。

建议长期坚持以下原则：

1. 以版本为节奏，而不是以脚本为节奏
2. 以稳定主线为核心，而不是一开始追求全覆盖
3. 以状态流 + 数据断言为主，UI 自动化为辅
4. 以 AI 总结和管理报告提升决策效率
5. 以里程碑分阶段推进，逐步形成长期质量资产

## 十二、结论

如果 DPU 项目希望真正实现“每次版本迭代都完成自动化测试，并由 AI 输出验证、问题、优化建议报告”，建议按本方案推进：

- 短期先建立核心测试包
- 中期打通版本自动触发
- 中后期引入 AI 自动报告
- 长期形成质量治理闭环

这样做的价值，不只是提高测试效率，更是让 DPU 项目的版本质量变得可视、可控、可管理。

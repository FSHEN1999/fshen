# DPU 项目自动化测试 / AI 测试方案

## 1. 文档目的

本文档用于为 DPU 项目制定一套可执行的自动化测试与 AI 测试建设方案，目标是：

- 基于现有 Python 脚本、Selenium UI 自动化、数据库校验、Webhook Mock 能力，形成稳定的测试体系。
- 将当前“脚本可跑”的测试能力升级为“可回归、可追踪、可度量、可持续演进”的测试平台能力。
- 引入 AI 辅助测试能力，提升测试设计效率、缺陷发现能力、用例覆盖深度和回归分析效率。
- 为 SIT / UAT / DEV / PREPROD 等多环境提供统一测试策略，降低人工回归和线上风险。

## 2. 项目现状分析

从当前仓库能力看，DPU 项目已经具备自动化测试建设的良好基础。

### 2.1 现有资产

- `mock_sit.py`、`mock_uat.py`
  - 已具备注册、SP 授权、underwriting、approval、PSP、esign、drawdown、repayment 等核心业务状态模拟能力。
- `db_helper.py`
  - 已沉淀常用数据库查询方法，可用于自动化断言和测试数据追踪。
- `webhook_service.py`
  - 已具备统一 webhook 发送能力，适合作为接口测试和状态驱动测试的基础。
- `locators.py`、`ui_helpers.py`、`js_scripts.py`
  - 已具备页面对象、UI 操作封装、前端脚本辅助能力，适合做 UI 自动化标准化。
- 各类 UI 自动化脚本
  - 如 `线下自动化.py`、`线上自动化.py`、`线下自动化hsbc.py` 等，说明项目已有端到端流程自动化基础。
- 单元测试文档与集成文档
  - 如 `UNIT_TEST_GUIDE.md`、`INTEGRATION_GUIDE.md`，说明仓库已开始往模块化和测试化方向建设。

### 2.2 当前痛点

结合项目形态，当前大概率存在以下问题：

- 自动化脚本偏“工具化”，但缺少统一测试分层与测试入口。
- 测试数据、环境配置、脚本执行、结果汇总尚未形成平台化闭环。
- UI 自动化容易受页面 locator 波动影响，维护成本较高。
- 业务状态链路长，跨系统依赖多，仅依赖 UI 回归成本高、定位慢。
- 用例设计可能仍偏人工经验，边界场景、异常场景、组合场景覆盖不稳定。
- 缺少基于变更影响分析、日志分析、失败聚类、智能回归推荐等 AI 能力。

### 2.3 结论

DPU 项目不适合只做单一层面的 UI 自动化，而应采用：

- 底层以单元测试 + 服务测试 + 数据校验为主
- 中层以接口 / webhook / mock 状态流测试为核心
- 上层以少量高价值 UI 端到端回归为保障
- 横向叠加 AI 测试设计、AI 日志分析、AI 智能回归、AI 缺陷发现能力

## 3. 建设目标

建议将建设目标分为三个阶段。

### 3.1 短期目标（1-2 个月）

- 建立统一测试目录、统一执行入口、统一测试报告。
- 优先覆盖 DPU 核心主流程自动化：
  - 注册
  - 授权
  - underwrite
  - approval
  - PSP
  - esign
  - drawdown
  - repayment
- 建立数据库断言能力和关键状态校验清单。
- 将现有核心脚本沉淀为可重复执行的回归套件。

### 3.2 中期目标（2-4 个月）

- 建设分层测试体系。
- 接入 CI/CD，支持按环境、按模块、按冒烟/回归分级执行。
- 接入 Allure 或 HTML 报告，沉淀历史执行趋势。
- 引入 AI 用例生成、失败归因、日志总结、回归推荐能力。

### 3.3 长期目标（4-8 个月）

- 建设 DPU 测试知识库和测试 Copilot。
- 基于代码变更、接口变更、Locator 变更、数据库字段变更自动推荐测试集。
- 建设 AI 驱动的异常场景探索测试与风险预测能力。
- 形成“需求 -> 用例 -> 自动化 -> 报告 -> 缺陷 -> 经验沉淀”的智能闭环。

## 4. 总体方案设计

## 4.1 测试架构分层

建议按以下 5 层建设。

### 第一层：单元测试层

目标：验证工具类、配置类、辅助模块的稳定性。

适用范围：

- `config.py`
- `exceptions.py`
- `webhook_service.py`
- `js_scripts.py`
- `db_helper.py`
- 输入校验、参数组装、状态转换逻辑

建议：

- 使用 `pytest` 统一管理。
- 对外部依赖全部 mock。
- 要求执行快、稳定、可在本地和 CI 中秒级完成。

### 第二层：服务/组件测试层

目标：验证 DPU Mock Service、Webhook 请求组装、数据库访问、状态转换方法的正确性。

适用范围：

- `mock_sit.py`
- `mock_uat.py`
- `DatabaseExecutor` / `DatabaseHelper`
- Webhook payload 构造逻辑
- DPU 状态枚举与状态推进逻辑

建议：

- 对数据库、HTTP 请求进行 mock 或半真实环境验证。
- 重点覆盖状态机、字段映射、异常分支、失败原因码。
- 建立“输入 -> 调用 -> 数据变化 -> 输出”闭环断言。

### 第三层：接口 / 状态流测试层

目标：验证 DPU 主流程各状态流转是否正确，确保核心业务链路在服务层可回归。

推荐重点覆盖：

- 用户注册成功链路
- updateOffer 回调链路
- underwriting APPROVED / REJECTED
- approval APPROVED / RETURNED
- PSP start / completed / fail
- esign SUCCESS / FAIL
- drawdown APPROVED / REJECTED + failure reason
- repayment Start / Success / Failure
- 多店铺绑定场景

建议：

- 以 `mock_sit.py`、`mock_uat.py` 为主要执行器。
- 每个场景同时校验：
  - HTTP 返回
  - 数据库状态
  - 关键 ID 是否生成
  - 下游状态是否按预期推进

### 第四层：UI 自动化层

目标：验证用户真实端到端流程，确保关键页面交互无回归。

建议只保留高价值场景，不追求全量 UI 自动化。

推荐保留的 UI 自动化场景：

- 新用户注册主流程
- 验证码校验与密码设置
- 公司信息填写
- 董事信息填写
- 银行账户填写
- 联系信息填写
- 3PL / PSP 关键跳转
- 核心审批结果页校验

建议：

- 统一基于 `locators.py` 和页面对象模式维护元素。
- 将通用操作沉淀到 `ui_helpers.py`。
- 对不稳定页面增加截图、HTML 快照、失败日志采集。
- 对 locator 变更建立集中维护机制，避免脚本内散落 XPath。

### 第五层：业务回归与环境验收层

目标：面向 SIT/UAT/PREPROD 提供一键冒烟与回归。

建议测试集分级：

- Smoke
  - 注册、核心状态推进、基础数据库连通
- Sanity
  - 单模块变更验证
- Regression
  - 全流程、异常流、多环境、多币种、多店铺
- Release Gate
  - 上线前强制执行的关键场景包

## 4.2 测试执行架构

建议建立统一执行入口，例如：

- `pytest` 作为主测试框架
- `tests/unit/`
- `tests/service/`
- `tests/api/`
- `tests/ui/`
- `tests/regression/`

并建立统一命令：

```bash
pytest tests/unit -m smoke
pytest tests/api -m uat
pytest tests/ui -m critical
pytest tests/regression --env=uat --html=report.html
```

建议补充统一配置：

- 环境参数：`--env=sit|uat|dev|preprod`
- 浏览器参数：`--headless`
- 报告目录参数
- 测试数据前缀参数
- 是否执行数据库断言参数

## 5. AI 测试方案

AI 测试不建议替代原有自动化，而应作为“增强层”叠加在现有自动化体系之上。

## 5.1 AI 测试在 DPU 的适用方向

### 方向一：AI 辅助用例设计

用途：

- 根据需求文档、接口文档、状态机规则自动生成测试点。
- 自动补全边界条件、异常路径、组合路径。
- 输出冒烟、回归、异常、兼容性测试集建议。

适合 DPU 的场景：

- 不同额度区间流程差异
- APPROVED / REJECTED / RETURNED 分支
- 多店铺绑定与分阶段通知场景
- repayment start / success / failure 连续状态场景
- drawdown failure reason 全枚举覆盖

落地方式：

- 输入：需求说明、状态枚举、API 配置、数据库表关系、历史缺陷
- 输出：标准化测试点列表、优先级、前置条件、断言点

### 方向二：AI 辅助脚本生成

用途：

- 自动生成 pytest 用例骨架。
- 自动补齐 mock 数据、请求体模板、数据库断言模板。
- 自动将自然语言测试步骤转为页面对象调用代码。

适合 DPU 的场景：

- webhook 场景用例模板批量生成
- drawdown / repayment 参数组合测试模板生成
- UI 页面对象脚本草稿生成

注意：

- AI 生成的脚本必须经过人工 review。
- 禁止直接把 AI 生成代码不校验地投入回归主线。

### 方向三：AI 辅助失败分析

用途：

- 读取失败日志、接口响应、截图、数据库状态，自动归纳失败原因。
- 区分脚本问题、环境问题、数据问题、产品缺陷、locator 失效。

适合 DPU 的场景：

- UI 自动化失败归因
- webhook 返回非 200 分析
- 数据库状态未推进问题排查
- 多环境配置差异问题识别

落地方式：

- 汇总测试日志、截图、请求响应、SQL 查询结果
- 由 AI 输出：
  - 失败摘要
  - 最可能根因
  - 关联模块
  - 建议排查顺序

### 方向四：AI 智能回归推荐

用途：

- 根据代码变更内容自动推荐应执行的测试集。

适合 DPU 的场景：

- `locators.py` 变更 -> 推荐 UI 冒烟 + 关键页面回归
- `webhook_service.py` 变更 -> 推荐 webhook / 状态流测试
- `db_helper.py` 变更 -> 推荐数据库断言测试
- `mock_uat.py` / `mock_sit.py` 变更 -> 推荐全链路状态流测试

### 方向五：AI 探索式测试

用途：

- 基于业务规则自动探索未覆盖场景和异常组合。

适合 DPU 的方向：

- 金额边界值
- 手机号/邮箱/商户信息非法输入
- 状态推进顺序错误
- 重复回调 / 幂等性异常
- 多次 repayment / drawdown 竞争场景

## 5.2 AI 测试能力分级

建议分三阶段引入。

### 阶段 A：AI 助手型

特点：

- 帮测试人员生成测试点、测试数据、脚本草稿、失败总结。
- 人主导，AI 辅助。

优点：

- 风险低
- 上手快
- ROI 高

### 阶段 B：AI 协同型

特点：

- AI 接入测试流水线，参与日志分析、回归推荐、报告总结。

优点：

- 降低测试分析成本
- 提升反馈速度

### 阶段 C：AI 智能代理型

特点：

- AI 能依据变更和环境状态自主生成并执行部分测试任务，再输出报告。

前提：

- 测试资产标准化程度较高
- 日志和结果结构化程度较高

## 6. DPU 项目推荐测试范围

## 6.1 核心业务主链路

必须优先自动化覆盖：

- 注册
- 生成 offerId
- validate SMS
- signup
- redirect 激活
- SP 授权
- underwriting
- approval
- PSP
- esign
- drawdown
- repayment

## 6.2 关键异常链路

必须优先补强：

- underwriting rejected
- approval returned
- approval rejected
- PSP fail / 中断
- esign fail
- drawdown rejected
- drawdown failure reason ER001-ER005
- repayment failure
- 数据不存在或关键 ID 缺失
- webhook 超时、异常返回、重复回调

## 6.3 高风险专项

建议纳入专项自动化或 AI 重点分析：

- 多店铺绑定流程
- 多环境配置差异
- 数据库重连与超时处理
- 幂等性 key 逻辑
- 线上 / 线下脚本差异
- HSBC 与非 HSBC 流程差异
- 生产迁移类脚本的安全校验

## 7. 建议的实施路线图

## 7.1 第一阶段：测试资产标准化

目标：把当前“散脚本”整理成“标准测试资产”。

工作项：

- 统一测试目录结构
- 统一 pytest 执行入口
- 增加 `conftest.py`、环境 fixture、数据 fixture
- 统一日志、截图、报告输出目录
- 给核心模块补充基础单元测试
- 给 `mock_sit.py` / `mock_uat.py` 提炼可复用调用层

交付物：

- 统一测试框架
- 基础 smoke 集
- HTML/Allure 报告

## 7.2 第二阶段：核心链路自动化覆盖

目标：建立 DPU 核心回归套件。

工作项：

- 建立主流程接口 / 状态流测试
- 建立数据库断言清单
- 选取高价值 UI 冒烟场景
- 建立异常分支测试包
- 支持 SIT/UAT 双环境切换执行

交付物：

- 主流程回归套件
- 异常流回归套件
- 环境级 smoke 验收包

## 7.3 第三阶段：AI 测试接入

目标：提升设计效率和分析效率。

工作项：

- 建立测试点生成 Prompt 模板
- 建立失败日志总结 Prompt 模板
- 建立变更影响 -> 回归推荐规则
- 接入测试报告 AI 总结
- 建立历史缺陷知识库

交付物：

- AI 测试助手
- AI 失败分析报告
- AI 回归推荐清单

## 7.4 第四阶段：测试平台化

目标：形成持续测试能力。

工作项：

- CI/CD 集成
- 定时回归
- 环境健康检查
- 测试结果趋势看板
- 缺陷 / 失败原因分类统计

交付物：

- 持续回归流水线
- 质量度量看板
- 发布准入机制

## 8. 推荐技术实现

## 8.1 技术栈建议

- 测试框架：`pytest`
- Mock：`unittest.mock` / `pytest-mock`
- 报告：`allure-pytest` 或 `pytest-html`
- 数据驱动：`pytest.mark.parametrize`
- UI 自动化：`selenium`
- 配置管理：`.env` + 配置类
- 日志采集：结构化日志 + 截图 + HTML dump
- CI：Jenkins / GitLab CI / GitHub Actions 任一可用平台
- AI 能力：
  - 用于测试点生成
  - 用于失败日志总结
  - 用于回归推荐
  - 用于缺陷聚类分析

## 8.2 推荐目录结构

```text
dpu/
├─ tests/
│  ├─ unit/
│  ├─ service/
│  ├─ api/
│  ├─ ui/
│  ├─ regression/
│  ├─ fixtures/
│  └─ data/
├─ reports/
├─ logs/
├─ screenshots/
├─ conftest.py
└─ pytest.ini
```

## 8.3 建议补充能力

- 测试标签机制
  - `smoke`
  - `critical`
  - `regression`
  - `uat`
  - `sit`
  - `ui`
  - `api`
- 失败自动截图
- 失败自动导出请求体和响应体
- 数据库断言封装
- 测试数据自动清理和追踪

## 9. 质量度量指标

方案落地后，建议持续跟踪以下指标：

- 自动化用例数量
- 核心流程自动化覆盖率
- 冒烟执行时长
- 回归执行时长
- 自动化通过率
- 假失败率
- locator 失效率
- 缺陷提前发现率
- AI 生成测试点采纳率
- AI 分析结果准确率

## 10. 组织与角色建议

建议角色分工如下：

- 测试负责人
  - 负责测试分层、质量目标、优先级、发布准入
- 自动化测试工程师
  - 负责框架建设、脚本开发、回归套件维护
- 业务测试工程师
  - 负责场景梳理、断言规则、异常流设计
- 开发工程师
  - 负责可测试性优化、日志完善、接口可观测性增强
- AI 测试推进人
  - 负责 Prompt 模板、知识库、失败分析闭环建设

## 11. 风险与应对

### 风险一：UI 自动化维护成本高

应对：

- 限制 UI 自动化范围，只覆盖高价值主链路。
- 统一 locator 来源，减少脚本散落。

### 风险二：环境不稳定导致假失败

应对：

- 区分环境问题与产品问题。
- 增加环境健康检查、前置校验、失败重试策略。

### 风险三：测试数据污染

应对：

- 建立专用测试账号规则。
- 标准化测试数据生成与清理机制。

### 风险四：AI 输出不稳定

应对：

- AI 只做增强，不直接替代最终判断。
- 关键用例、关键脚本、关键结论必须人工 review。

## 12. 最终建议

对于 DPU 项目，最优解不是单独建设“自动化测试”，也不是单独建设“AI 测试”，而是建设：

“分层自动化测试体系 + AI 增强测试能力”的组合方案。

具体建议如下：

- 先把现有脚本标准化，形成稳定的 pytest 回归框架。
- 再优先打通核心状态流自动化和数据库断言闭环。
- UI 层只保留关键高价值链路。
- AI 优先用于测试点生成、失败分析、回归推荐，不宜一开始就做全自动智能代理。
- 以 SIT/UAT 为主战场逐步落地，最终形成发布准入能力。

## 13. 建议的近期落地清单

如果按可执行优先级排序，建议下一步直接做这 8 件事：

1. 建立 `tests/` 标准目录和 `pytest` 统一入口。
2. 为 `webhook_service.py`、`db_helper.py`、`config.py` 补齐基础单元测试。
3. 将 `mock_sit.py`、`mock_uat.py` 的核心状态流提炼为可复用测试调用层。
4. 建立注册、underwriting、approval、PSP、drawdown、repayment 的核心回归用例。
5. 封装统一数据库断言工具。
6. 选 3-5 条最关键 UI 场景作为冒烟集。
7. 建立测试报告与失败截图归档机制。
8. 建立 AI 测试助手模板，用于测试点生成和失败分析。

---

如果需要，这份文档下一步可以继续扩展成以下两个版本：

- 管理层版本：更偏目标、收益、投入、阶段里程碑
- 执行落地版本：更偏目录结构、脚本规范、pytest 设计、CI 接入清单、AI Prompt 模板

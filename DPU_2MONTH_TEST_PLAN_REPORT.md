# DPU 项目自动化测试 / AI 测试两个月建设方案

## 一、项目背景

当前 DPU 项目已经具备一定自动化基础，包括：

- `mock_sit.py`、`mock_uat.py` 提供状态流模拟能力
- `线上自动化.py`、`线下自动化.py`、`线下自动化hsbc.py` 提供 UI 自动化能力
- `db_helper.py` 提供数据库查询与校验能力

但现阶段自动化能力仍以脚本为主，尚未形成稳定、聚焦、可持续复用的回归体系。随着迭代推进，若继续依赖人工回归，将面临以下问题：

- 核心流程回归成本高
- 状态流链路长，人工验证效率低
- HSBC、多店铺、线上/线下等分支流程易遗漏
- 变更后无法快速判断对稳定功能的影响范围

因此，短期需要在有限时间内，优先围绕稳定功能建立一套“小而有效”的自动化回归能力。

## 二、建设目标

本阶段周期为 2 个月，建设目标不是覆盖全部功能，而是优先覆盖当前迭代中稳定、关键、可复用的主流程，形成可执行的冒烟和回归测试能力。

本阶段目标如下：

1. 建立 DPU 核心稳定流程的自动化回归包
2. 形成线上、线下、HSBC、多店铺等关键分支的基础覆盖
3. 建立“动作执行 + 数据库断言”的闭环验证能力
4. 形成固定回归入口，支持迭代后快速验证
5. 引入轻量 AI 测试辅助能力，提升测试设计和失败分析效率

## 三、本阶段覆盖范围

本阶段建议只覆盖以下 5 类稳定流程：

### 1. FP-USD 主流程

覆盖内容：

- 注册
- SP 授权
- updateOffer
- link-sp-3pl
- underwritten
- approved offer
- PSP start / completed
- esign
- drawdown

这是本阶段优先级最高的主流程，也是最适合作为回归主线建设的对象。

### 2. HSBC 主流程

覆盖内容：

- HSBC 页面入口
- 注册流程
- SP 授权
- updateOffer
- link-sp-3pl
- HSBC 专用 PSP start / completed
- 后续关键状态流

HSBC 流程存在独立入口、独立页面元素以及专用 PSP 逻辑，应作为单独测试包建设。

### 3. 多店铺流程

覆盖内容：

- 多店铺 SP 绑定
- 多店铺 3PL redirect
- PSP 绑定
- 额度拆分逻辑

多店铺流程复杂度较高，且存在独立脚本和 SOP，建议作为专项建设。

### 4. 线上流程

覆盖内容：

- 线上 FP 主流程
- 稳定 TIER 分支的关键路径

目标是形成一套高价值冒烟能力，而不是覆盖全部页面与全部分支。

### 5. 线下流程

覆盖内容：

- 线下普通流程
- 线下 HSBC 流程

重点保证入口稳定、关键页面稳定、主链路可执行。

## 四、本阶段不纳入范围

为保证两个月内可以落地，本阶段不建议纳入以下内容：

- 全量币种、全量产品、全量分支覆盖
- 所有异常流和失败码全覆盖
- 所有历史脚本统一重构
- 完整测试平台化建设
- 全自动 AI Agent 测试执行体系
- 所有页面的全量 UI 自动化

原则是先保核心、先保稳定、先保可回归。

## 五、建设策略

结合 DPU 项目特点，建议采用以下策略：

### 1. 服务层优先

以 `mock_sit.py`、`mock_uat.py` 的状态流能力为核心，优先覆盖：

- underwritten
- approved offer
- PSP
- esign
- drawdown
- repayment
- 多店铺
- HSBC PSP

原因是 DPU 的核心风险点主要在状态流和数据落库，而不只是 UI 页面。

### 2. UI 层聚焦

UI 自动化只覆盖高价值主链路，不追求全量页面覆盖。

重点保留：

- 注册入口
- 授权关键步骤
- 核心信息填写页面
- 最终申请动作

### 3. 数据库断言必须引入

本阶段自动化必须引入数据库断言，确保不仅“脚本执行成功”，还要验证：

- merchant_id / application_id 是否生成
- 授权记录是否写入
- 关键状态是否推进
- drawdown / repayment 等核心数据是否落库

### 4. 分包建设

建议按业务主线分包，而不是按技术手段分包。

即按以下 5 个包推进：

- FP-USD 主流程包
- HSBC 主流程包
- 多店铺专项包
- 线上冒烟包
- 线下冒烟包

## 六、两个月实施计划

### 第 1 阶段：第 1-2 周

目标：

- 明确测试边界
- 梳理稳定主线
- 确定统一执行方式

输出：

- 场景清单
- 优先级清单
- 初版执行入口

### 第 2 阶段：第 3-4 周

目标：

- 优先打通 FP-USD 主流程自动化

输出：

- FP-USD 主流程回归包
- 基础数据库断言清单
- 初版执行报告

### 第 3 阶段：第 5-6 周

目标：

- 完成 HSBC 主流程
- 完成线下稳定流程

输出：

- HSBC 回归包
- 线下冒烟包

### 第 4 阶段：第 7 周

目标：

- 完成多店铺专项覆盖

输出：

- 多店铺专项测试包
- 多店铺关键校验点

### 第 5 阶段：第 8 周

目标：

- 试运行
- 整理回归入口
- 形成阶段成果输出

输出：

- 固定回归方式
- 阶段建设总结
- 下一阶段建议

## 七、AI 测试在本阶段的定位

本阶段 AI 测试只作为“辅助增强能力”，不作为主执行方式。

建议主要用于以下 3 个方向：

### 1. AI 辅助测试点梳理

用于快速梳理：

- FP-USD 主流程测试点
- HSBC 差异测试点
- 多店铺专项测试点
- 线上 / 线下差异点

### 2. AI 辅助失败分析

用于分析：

- UI 自动化失败日志
- webhook 返回异常
- 数据库断言失败

### 3. AI 辅助回归推荐

根据变更内容，推荐对应测试包，例如：

- `locators.py` 变更 -> 推荐 UI 冒烟
- `mock_uat.py` / `mock_sit.py` 变更 -> 推荐状态流回归
- 多店铺脚本变更 -> 推荐多店铺专项包

## 八、预期收益

若本方案按期完成，预计可带来以下收益：

### 1. 降低回归成本

将核心稳定流程从人工验证转为自动化执行，缩短每轮迭代的验证时间。

### 2. 提高问题发现效率

通过状态流和数据库断言，可以更快定位问题是在页面、接口、状态推进还是数据落库环节。

### 3. 降低分支流程遗漏风险

对 HSBC、多店铺、线上/线下等关键分支建立基础覆盖，减少因流程差异导致的问题遗漏。

### 4. 支撑后续扩展

本阶段完成后，可以在此基础上继续扩展异常流、更多币种、更多分支以及更完整的 AI 测试能力。

## 九、阶段性成果定义

两个月结束时，建议以以下成果作为验收标准：

1. 已形成 FP-USD 主流程自动化回归包
2. 已形成 HSBC 主流程自动化回归包
3. 已形成多店铺专项测试包
4. 已形成线上 / 线下冒烟包
5. 已形成数据库断言清单
6. 已形成统一执行说明
7. 已形成 AI 辅助测试点和失败分析模板

## 十、结论

对 DPU 项目而言，短期最合理的路径不是追求“大而全”，而是围绕稳定功能先建立可执行、可复用、可持续迭代的自动化回归能力。

建议本阶段聚焦：

- FP-USD 主流程
- HSBC 主流程
- 多店铺专项
- 线上 / 线下冒烟

并以“服务层状态流 + UI 关键路径 + 数据库断言 + AI 轻量辅助”的方式推进。

这套方案更符合当前 DPU 项目实际，也更适合在两个月内真正落地。

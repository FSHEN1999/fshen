# DPU 项目 2 个月自动化测试 / AI 测试短期方案

## 1. 目标

本阶段目标不是覆盖 DPU 全部功能，而是在 2 个月内，围绕当前迭代里相对稳定、价值最高、可重复执行的流程，建设一套可落地的自动化测试能力。

重点覆盖范围：

- FP-USD 主流程
- HSBC 流程
- 多店铺流程
- 线上流程
- 线下流程

目标结果：

- 能形成一套稳定的冒烟回归包
- 能支持 SIT / UAT / PREPROD 的基础回归
- 能覆盖核心状态流和关键数据库断言
- 能在变更后快速判断是否影响稳定主流程

## 2. 对 DPU 项目的理解

结合当前仓库代码，DPU 项目本质上是“注册 + 授权 + 状态流推进 + 数据落库校验”的组合系统，不是单纯的 UI 自动化项目。

当前可以分成 4 类核心能力：

### 2.1 状态模拟能力

以 `mock_sit.py`、`mock_uat.py` 为核心，已经具备以下能力：

- link-sp-3pl 关联
- underwritten
- approved offer
- psp start / psp completed
- esign
- drawdown
- repayment start / repayment
- 多店铺 SP 绑定
- 多店铺 3PL redirect
- HSBC 专用 PSP start / completed

这说明 DPU 的核心业务状态流，已经可以不依赖 UI，直接从服务层做自动化。

### 2.2 UI 自动化能力

当前仓库中已经有几类 UI 自动化脚本：

- `线上自动化.py`
- `线下自动化.py`
- `线下自动化hsbc.py`
- `线下自动化hsbc手动输入版.py`

从脚本结构看，UI 自动化主要承担：

- 注册流程
- SP 授权跳转
- 页面信息填写
- 银行信息填写
- 最终申请动作触发

其中线上 / 线下 / HSBC 的入口不同，但核心链路类似。

### 2.3 数据校验能力

项目已具备数据库辅助能力：

- `db_helper.py`
- `DatabaseExecutor` 相关逻辑

可用于校验：

- merchant_id
- application_unique_id
- fund_application_id
- authorization_id
- loan_id
- idempotency_key
- platform_offer_id

这意味着自动化可以做到“动作执行 + 数据断言”闭环，而不是只看页面是否点击成功。

### 2.4 特殊业务分支

这是 DPU 项目最值得重点关注的部分。

#### FP-USD 流程

从 `线上自动化.py`、`线下自动化.py` 来看，USD 是当前默认和主用流程之一，金额配置、approval、esign、drawdown 等都有明确映射。

#### HSBC 流程

HSBC 不是简单换 URL，而是存在单独页面入口、专用 UI 元素、专用 PSP 通知分支，`mock_uat.py` 里也单独维护了 HSBC PSP 的 pending / completed 逻辑。

#### 多店铺流程

多店铺不只是多一个页面，而是包含：

- SP 店铺绑定
- 3PL redirect
- PSP 绑定
- 额度拆分

对应脚本和 SOP 已经存在，说明这是独立专项，不建议混在普通主流程里一起做。

#### 线上 / 线下流程

线上和线下的差异主要体现在：

- 入口不同
- offerId / redirect 方式不同
- 某些页面分支和跳转行为不同
- 批准、PSP、drawdown 的推进策略略有差异

因此测试方案必须按“业务主线”分组，而不是按“技术类型”分组。

## 3. 两个月内建议覆盖的范围

短期只覆盖稳定主线，不追求全量。

建议分 5 个测试包。

### 包 1：FP-USD 主流程回归包

覆盖内容：

- 注册
- SP 授权
- updateOffer
- link-sp-3pl
- underwritten
- approved offer
- psp start
- psp completed
- esign
- drawdown

重点断言：

- 关键 ID 成功生成
- 关键 webhook 请求成功
- drawdown 状态推进成功
- 数据库状态与页面路径一致

这是 2 个月内优先级最高的主线。

### 包 2：HSBC 主流程回归包

覆盖内容：

- HSBC 入口页
- 注册流程
- SP 授权
- updateOffer
- link-sp-3pl
- HSBC 专用 PSP start
- HSBC 专用 PSP completed
- esign 或后续主链路关键节点

重点断言：

- HSBC 页面入口可用
- HSBC 分支 locator 正常
- HSBC PSP 逻辑和普通 PSP 分开校验
- merchantAccountId 关联逻辑正确

### 包 3：多店铺流程专项包

覆盖内容：

- 多店铺 SP 绑定
- 多店铺 3PL redirect
- PSP 绑定脚本能力
- 额度拆分逻辑

重点断言：

- 多店铺授权成功
- 多 merchant_account_limit 记录处理正确
- PSP 状态更新成功
- limit 拆分结果正确

这个包建议独立，不和普通主流程混跑。

### 包 4：线下流程冒烟包

覆盖内容：

- 线下普通流程主链路
- 线下 HSBC 主链路

重点断言：

- 固定入口页可访问
- 关键页面字段可正常填写
- 关键按钮和跳转稳定
- 最终申请动作成功触发

### 包 5：线上流程冒烟包

覆盖内容：

- 线上 FP 流程
- 线上 TIER 分支里的稳定路径

重点断言：

- offerId 生成成功
- redirect 成功
- SP 授权成功
- 后续状态流触发成功

## 4. 不建议在 2 个月内做的范围

为了保证落地，以下内容不建议纳入本阶段主目标：

- 全币种全场景覆盖
- 全量异常流自动化
- 所有失败原因码自动化
- 所有历史脚本统一重构
- 完整 AI Agent 自主测试平台
- 所有页面全量 UI 自动化

这些内容可以留到第二阶段。

## 5. 测试策略

短期建议采用“服务层为主，UI 层为辅”的策略。

### 5.1 主策略

- 状态流测试以 `mock_sit.py` / `mock_uat.py` 为主
- UI 自动化只保留高价值入口和关键页面
- 数据库断言必须作为核心能力引入
- 线上 / 线下 / HSBC / 多店铺分开建包

### 5.2 原因

因为 DPU 的核心风险并不只在页面，而在：

- 状态流是否推进成功
- webhook 是否正确发送
- 数据库是否正确落库
- 多分支流程是否走错

如果只做 UI 自动化，维护成本高，定位也慢。

## 6. 两个月实施计划

## 第 1-2 周：梳理与标准化

目标：

- 明确 5 个测试包边界
- 统一脚本入口
- 确定测试环境和测试数据规则

输出：

- 测试清单
- 场景优先级
- 基础目录和执行方式

## 第 3-4 周：先打通 FP-USD 主流程

目标：

- 打通最稳定、最核心的 FP-USD 主流程

输出：

- FP-USD 冒烟回归包
- 数据库断言清单
- 基础报告输出

## 第 5-6 周：补 HSBC 与线下流程

目标：

- 打通 HSBC 主流程
- 打通线下稳定流程

输出：

- HSBC 主流程测试包
- 线下冒烟测试包

## 第 7 周：补多店铺专项

目标：

- 覆盖多店铺 SP 绑定、3PL redirect、额度拆分

输出：

- 多店铺专项测试包
- 多店铺关键断言清单

## 第 8 周：收口与试运行

目标：

- 整理回归入口
- 试跑一轮 SIT / UAT
- 形成固定回归节奏

输出：

- 2 个月阶段成果报告
- 回归执行说明
- 第二阶段建议清单

## 7. AI 测试在本阶段怎么用

本阶段 AI 不做“全自动执行”，只做增强。

建议只用在 3 个方向：

### 7.1 AI 辅助测试点梳理

用于：

- 根据 FP、HSBC、多店铺、线上线下流程整理测试点
- 自动补全边界条件和遗漏场景

### 7.2 AI 辅助失败分析

用于：

- 分析 UI 自动化失败日志
- 分析 webhook 返回异常
- 分析数据库断言失败原因

### 7.3 AI 辅助回归推荐

用于：

- `locators.py` 变更后推荐跑 UI 冒烟
- `mock_uat.py` / `mock_sit.py` 变更后推荐跑状态流回归
- 多店铺脚本变更后推荐跑多店铺专项包

## 8. 最终建议

如果按 2 个月的现实目标来看，DPU 项目最合适的方案是：

- 不做大而全
- 先抓稳定主线
- 先抓 FP-USD
- 再补 HSBC
- 多店铺单独作为专项
- 线上 / 线下只做高价值冒烟
- AI 先服务于测试设计和失败分析

## 9. 建议的短期交付物

2 个月结束时，建议至少拿到以下结果：

1. 一套 FP-USD 主流程自动化回归包
2. 一套 HSBC 主流程自动化回归包
3. 一套多店铺专项测试包
4. 一套线上 / 线下冒烟包
5. 一份数据库断言清单
6. 一套统一执行说明
7. 一套 AI 辅助测试点 / 失败分析模板

